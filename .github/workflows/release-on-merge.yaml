---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Release on Merge"
on:
  push:
    branches: ["main"]
    paths:
      - "apps/**"

concurrency:
  group: release-image
  cancel-in-progress: false

jobs:
  diff:
    uses: kusold/containers/.github/workflows/diff.yaml@5f1bc8f02c7217915f16e5b5fe9db53ee3a44e96 # main

  build-images:
    needs: ["diff"]
    if: ${{ !contains(needs.diff.outputs.matrix, '[]') }}
    uses: kusold/containers/.github/workflows/build-images.yaml@5f1bc8f02c7217915f16e5b5fe9db53ee3a44e96 # main
    # uses: ./.github/workflows/build-images.yaml
    secrets: inherit
    with:
      matrix: "${{ needs.diff.outputs.matrix }}"
      publish: true

  scan-images:
    needs: ["diff", "build-images"]
    if: ${{ !contains(needs.diff.outputs.matrix, '[]') }}
    uses: kusold/containers/.github/workflows/trivy-scan.yaml@5f1bc8f02c7217915f16e5b5fe9db53ee3a44e96 # main
    secrets: inherit
    permissions:
      contents: read
      packages: read
      security-events: write
