---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Release on Merge"
on:
  push:
    branches: ["main"]
    paths:
      - "apps/**"

concurrency:
  group: release-image
  cancel-in-progress: false

jobs:
  diff:
    uses: kusold/containers/.github/workflows/diff.yaml@958853a4d7fc68cde8fc1b39cb54312a0f1a620a # main

  build-images:
    needs: ["diff"]
    if: ${{ !contains(needs.diff.outputs.matrix, '[]') }}
    uses: kusold/containers/.github/workflows/build-images.yaml@958853a4d7fc68cde8fc1b39cb54312a0f1a620a # main
    # uses: ./.github/workflows/build-images.yaml
    secrets: inherit
    with:
      matrix: "${{ needs.diff.outputs.matrix }}"
      publish: true

  scan-images:
    needs: ["diff", "build-images"]
    if: ${{ !contains(needs.diff.outputs.matrix, '[]') }}
    uses: kusold/containers/.github/workflows/trivy-scan.yaml@958853a4d7fc68cde8fc1b39cb54312a0f1a620a # main
    secrets: inherit
    permissions:
      contents: read
      packages: read
      security-events: write
